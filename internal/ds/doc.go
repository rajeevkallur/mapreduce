// Package ds provides small data-structure helpers and utilities.
//
// It includes a FIFO queue (QueueInt) and a LIFO stack (StackInt) of integers,
// as well as conversion of integers to their English words representation
// (NumberToWords).
package ds
